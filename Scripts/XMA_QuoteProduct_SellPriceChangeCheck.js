function setPriceChangeFlag(executionContext)
{
    /*
    const formContext = executionContext.data 
                            ? executionContext 
                            : executionContext.getFormContext();

    var quoteProductId = formContext.data.entity.getId();
    var sellPrice = formContext.getAttribute("priceperunit").getValue();
    var sellPriceOrig = formContext.getAttribute("igl_originalpeprice").getValue();
    var priceChageStatus = formContext.getAttribute("xma_pricechanged").getValue();
    
    
    if (sellPrice != sellPriceOrig) {
        formContext.getAttribute("xma_issellpricemanuallychanged").setValue(true);        
        formContext.getAttribute("xma_issellpricemanuallychanged").setSubmitMode("always");
        formContext.getAttribute("xma_pricechanged").setValue(2);
        formContext.getAttribute("xma_pricechanged").setSubmitMode("always");        
    }
    else {
        formContext.getAttribute("xma_issellpricemanuallychanged").setValue(true);
        formContext.getAttribute("xma_issellpricemanuallychanged").setSubmitMode("always");
        if (priceChageStatus == 1) {
            priceChageStatus = 2;
        }
        formContext.getAttribute("xma_pricechanged").setValue(priceChageStatus);
        formContext.getAttribute("xma_pricechanged").setSubmitMode("always");        
    }
    */
}